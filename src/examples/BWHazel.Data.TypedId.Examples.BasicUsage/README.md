# Example: Basic Usage

This example demonstrates how to create typed IDs, how they can interconvert between their underlying ID types and how they provide type safety.

## Running the Example

To run the example, run the following on the command-line:

```sh
dotnet run
```